{
  "name": "users",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --ci --reporters=default --reporters=jest-junit --outputFile=junit.xml"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0"
  }
}
